{
  "buildCommand": "([ -f convex/_generated/api.js ] && echo 'Convex files exist') || (bunx convex codegen || echo 'Convex codegen skipped') && bun run build",
  "devCommand": "bun run dev",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["syd1"]
}

